# =============================================================================
# JAM SERVER - PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=4000
LISTEN_IP=0.0.0.0

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Your frontend URL (no trailing slash)
CORS_ORIGIN=https://jam.khobordari.in

# -----------------------------------------------------------------------------
# MediaSoup Configuration
# -----------------------------------------------------------------------------
# Listen IP (0.0.0.0 = all interfaces)
MEDIASOUP_LISTEN_IP=0.0.0.0

# Your server's PUBLIC IP address (required in production)
# This is what clients will use to connect for WebRTC
MEDIASOUP_ANNOUNCED_IP=172.232.111.180

# -----------------------------------------------------------------------------
# WebRTC Port Range
# -----------------------------------------------------------------------------
# These ports must be open in your firewall for UDP (and optionally TCP)
# Each concurrent WebRTC connection uses ports from this range
RTC_MIN_PORT=40000
RTC_MAX_PORT=49999
